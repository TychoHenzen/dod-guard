# 90% Test Coverage on All Packages — Requirements Spec

<claude_instructions>
**For Claude (/goal):** Work through each incomplete task below.
1. Mark a task `[>]` when you begin working on it.
2. Call `dod_check` to verify proofs — do NOT mark proofs manually.
   While iterating on one subtree, pass `nodePath` to verify just that part fast (others are carried, not re-run). A scoped run returns INCOMPLETE, never PASS.
3. A task group is complete when ALL its concrete proofs pass via `dod_check`.
3b. For `manual`/`review` proofs: `dod_check` never auto-prompts — call
    `dod_verify(dod_id, proof_id)` explicitly when verification is actually relevant.
3c. **Manual verification is a HARD GATE.** DoD cannot PASS without it.
    Proofs can pass against wrong code. Visual verification catches what metrics miss.
4. Use `dod_refine` to turn a draft leaf into a concrete proof (mode=concretize) or subdivide into child tasks (mode=subdivide).
4b. **Refine incrementally per task group, not all at once.** Scoped dod_check is faster
    than full runs — use it. Refining 7 drafts at session end = rubber-stamping.
4c. Use `dod_add_node` to add new nodes discovered during implementation.
5. If a proof cannot be met, use `dod_amend` to modify it with a reason.
5b. **Amending a proof 3+ times is a red flag** — you're probably tuning proofs to pass
    rather than fixing the bug. Re-examine the approach.
5c. Proof commands run on the HOST OS — write OS-correct commands (no bash on Windows).
6. Continue until `dod_check` returns PASS (zero drafts, all proofs pass, manuals verified) — then stop and report done.
6b. **If the approach isn't working, stop and re-interview.** Don't silently pivot to
    a different implementation while keeping the old DoD. The DoD must match what you're doing.

**Self-contained.** All commands run from `C:\Users\siriu\mcp-servers\dod-guard` unless noted.

**🔒 Anti-cheat:** Proofs are stored canonically in MCP storage (dod-guard).
`dod_check` executes commands from the canonical copy, not this markdown file.
Editing proof text here has no effect on verification.
Store tampering is **logged and detectable** — each check prints a proof-set fingerprint.
Manual/review proofs are confirmed by the human directly (popup / elicitation) via `dod_verify` —
Claude cannot self-confirm them, and an unrequested one holds the DoD at INCOMPLETE, never PASS.
</claude_instructions>

**Goal:** Achieve ≥90% statement coverage on all 4 monorepo packages (dod-guard, evomcp, gitevo, obsidian-rag) using coverage-driven TDD, with CI gate enforcement.

**Date:** 2026-07-12
**Target:** `C:\Users\siriu\mcp-servers\dod-guard`
**DoD ID:** `4dd0e00c-1de8-4926-8eb0-50b4591775c7`
**Last check:** INCOMPLETE (2026-07-12T14:25:36.018Z)

---

## Decisions (locked with user)

<decisions>
## Design Decisions

1. **Coverage measurement**: Node.js built-in --experimental-test-coverage for immediate feedback; c8 for CI/scripting
2. **CI gate**: Separate CI step after tests, checks per-file thresholds
3. **OS-dependent code**: Mark untestable paths with c8 ignore comments + document in skip_reasons
4. **Test framework**: Node.js native test runner — no new dependencies
5. **Mock strategy**: mock.module for ESM modules, mock.method for specific functions
6. **Coverage target**: 90% statements per file (not per package aggregate)
7. **TDD approach**: Red phase first — write test, verify it fails, then implement
</decisions>

## Requirements

<requirements>
## Requirements

### Scope
- All 4 packages: evomcp, obsidian-rag, gitevo, dod-guard
- Priority: worst-first (evomcp 50% → obsidian-rag 55% → gitevo 87% → dod-guard 85-90%)

### Test Strategy
- Coverage-driven TDD: prioritize uncovered code paths
- Each test must fail initially (red phase) before implementation passes it
- Reason from user perspective — try to find real bugs

### Coverage Tooling
- Add c8 coverage scripts to evomcp, obsidian-rag, gitevo package.json
- dod-guard already has c8 configured
- Add CI coverage gate that fails if coverage drops below 90%

### OS-Specific Code
- Test testable parts, skip OS-only paths (message boxes, Obsidian app spawning)

### Baseline Coverage (source files only, 2026-07-12)
| Package | Est. Coverage | Worst Files |
|---------|-------------|-------------|
| dod-guard | ~85-90% | store.ts (48%), notify.ts (82%), test-metrics.ts (84%) |
| evomcp | ~50-55% | evolve.ts (37%), agent.ts (51%), solve.ts (0%) |
| gitevo | ~87% | index.ts (80%) |
| obsidian-rag | ~50-55% | vault.ts (21%), cli.ts (32%), indexer.ts (66%) |

All ~700 existing tests pass across all packages.
</requirements>

## Research Notes

<research_notes>
## Research Notes

### Codebase Structure
- 4 packages, monorepo with npm workspaces
- dod-guard: 21 source files, 16 test files, 544 tests (all pass)
- evomcp: 6 source files, 4 test files, 69 tests (all pass)
- gitevo: 2 source files, 2 test files, ~30 tests (all pass)
- obsidian-rag: 8 source files, 6 test files, 67 tests (all pass)

### Key Findings
- solve.ts test file only tests agent.ts helpers — solve() has 0% coverage
- evolve.test.ts only tests matchSimple helper — evolve() barely covered
- vault.ts and cli.ts heavily OS-dependent (CLI spawning, file system walking)
- notify.ts has broken Windows messagebox pathway (known from prior evo_learn)
- store.ts 48% — canonical storage operations, most testable
- test-metrics.ts 1107 lines — largest file, moderate gap at 84%

### Prior Learnings
- Windows proof commands must use backslashes for findstr paths
- ESM project breaks node -e require() in proof commands
- code-review-graph dead_code ~50% false positive
- Dod_verify messagebox broken on Windows — all verification must be automated
</research_notes>

## Open Questions

<open_questions>
## Open Questions

1. Coverage threshold: per-file 90% or per-package aggregate? → Decided: per-file 90%
2. OS-dependent files that can't reach 90%? → Test testable parts, document skips
3. Coverage badges in README? → Deferred, not in scope
</open_questions>

---

## Definition of Done

<definition_of_done>

### Coverage Infrastructure [x]

  - [x] Proof: `node -e "const p=require('./packages/evomcp/package.json'); const ok = p.scripts?.coverage && p.devDependencies?.c8; if (!ok) throw new Error('coverage script or c8 dep missing'); console.log('OK: coverage script present, c8 installed');"` → Add c8 coverage npm script and c8 devDependency to evomcp/package.json
  - [x] Proof: `node -e "const p=require('./packages/obsidian-rag/package.json'); const ok = p.scripts?.coverage && p.devDependencies?.c8; if (!ok) throw new Error('coverage script or c8 dep missing'); console.log('OK: coverage script present, c8 installed');"` → Add c8 coverage npm script and c8 devDependency to obsidian-rag/package.json
  - [x] Proof: `node -e "const p=require('./packages/gitevo/package.json'); const ok = p.scripts?.coverage && p.devDependencies?.c8; if (!ok) throw new Error('coverage script or c8 dep missing'); console.log('OK: coverage script present, c8 installed');"` → Add c8 coverage npm script and c8 devDependency to gitevo/package.json
  - [x] Proof: `node -e "const fs=require('fs'); const yml=fs.readFileSync('.github/workflows/npm-publish.yml','utf8'); const hasC8=yml.includes('c8')&&yml.includes('coverage')&&yml.includes('--statements'); if(!hasC8) throw new Error('coverage CI step missing'); console.log('OK: CI coverage gate present');"` → Add CI coverage gate step to static-analysis job that runs c8 coverage with 90% threshold per package and fails if threshold not met

### evomcp Test Coverage (50% → 90%) [~]

  - [x] Proof: `npx c8 --include="packages/evomcp/src/evolve.ts" --exclude="packages/evomcp/src/evolve.test.ts" --statements 90 --reporter=text node --experimental-test-module-mocks --test "packages/evomcp/dist/evolve.test.js"` → evolve.ts achieves ≥90% statement coverage via TDD tests. Covers: evolve() orchestration, readTargetFiles, saveState/restoreState, captureState, applyPatch, matchSimple edge cases, elite selection, generation loop.
  - [~] **Draft**: TDD tests for agent.ts uncovered: getBackendApiKey(), resolveApiKey(), apiKeySource(), checkProxyHealth(), ensureProxy(), spawnClaude(), spawnClaudeN(), runCommand(), extractScore(), mutationPrompt(). Currently 51%.
  - [~] **Draft**: TDD tests for solve.ts: solve() orchestration, captureDiff(), repair loop, stuck detection, lineage killing, escalation report. Currently 0% — test file only tests agent.ts helpers.

### obsidian-rag Test Coverage (55% → 90%) [~]

  - [~] **Draft**: TDD tests for vault.ts testable pure functions: extractWikilinks(), memoryDir(), readNoteMeta(). Mock fs for walkVault/readNote/writeNote. Skip OS-only Obsidian paths. Currently 21%.
  - [~] **Draft**: TDD tests for cli.ts: argument building, output parsing, error handling. Mock execSync/exec. Skip ensureObsidianRunning/cliAvailable. Currently 32%.
  - [~] **Draft**: TDD tests for indexer.ts: chunkMarkdown() heading-aware splitting, hashContent(), indexVault/reindexVault with store mocks. Currently 66%.

### gitevo Test Coverage (87% → 90%) [~]

  - [~] **Draft**: TDD tests for gitevo index.ts uncovered paths: MCP tool handler edge cases, error paths, wrap() function. Currently 80%. operations.ts already at 94%.

### dod-guard Test Coverage (85% → 90%) [~]

  - [~] **Draft**: TDD tests for store.ts: generateId(), save(), load(), loadRaw(), findByPath(), listAll(), migrateDoc(), listLegacyCount(), listAllRaw(), remove(). Mock fs and crypto. Currently 48%.
  - [~] **Draft**: TDD tests for test-metrics.ts uncovered branches: analyseTestMetrics() edge cases, scoreFromMetrics(), file system scanning. Currently 84% — 1107 lines.
  - [~] **Draft**: TDD tests for notify.ts testable logic. Skip Windows messagebox spawning (known broken). Test notification decision logic, fallback paths. Currently 82%.

### Code Quality [x]

  - [x] Proof: `npx @biomejs/biome check packages/dod-guard/src packages/evomcp/src packages/gitevo/src packages/obsidian-rag/src` → Biome lint check passes with zero errors across all packages
  - [x] Proof: `npx @biomejs/biome format packages/dod-guard/src packages/evomcp/src packages/gitevo/src packages/obsidian-rag/src` → Biome format check passes — all files properly formatted
  - [x] Proof: `npm test` → Full test suite passes — all 4 packages, all existing ~700 tests

### Manual Verification [x]

  - [~] Proof: `undefined` → Review all new test code: tests verify behavior, TDD red-green-refactor followed, no tautological tests
  - [~] Proof: Manual — Manually verify coverage ≥90% per file, no c8 ignore abuse, OS-dependent skips reasonable _(awaiting human verification)_

</definition_of_done>

## Open risks

<open_risks>
## Open Risks

1. **OS-dependent gaps**: vault.ts, cli.ts, notify.ts may not reach 90%. Mitigation: document skips.
2. **solve.ts complexity**: solve() spawns claude -p subprocesses — hard to unit test. Extract pure logic.
3. **test-metrics.ts size**: 1107 lines, many branches. Significant test effort.
4. **CI coverage gate**: May need tuning for different OS platforms.
5. **c8 compatibility**: Node experimental coverage vs c8 may produce different numbers.
</open_risks>

## Amendment log

- **2026-07-12T13:15:07.409Z** [__meta__] modified: Add missing skip_reasons for categories that don't apply to test-only changes: integration_wiring, integration_behavioral, complexity, coverage. integration_wiring and integration_behavioral don't apply because this is pure test addition with no production code changes. complexity doesn't apply because test code complexity isn't measured. coverage skip is because the coverage proof IS the DoD itself — checking coverage metrics is what the sub-problem proofs verify.
- **2026-07-12T13:36:51.062Z** [0.children.0] refined: Refined draft → concrete: Add c8 coverage npm script and c8 devDependency to evomcp/package.json
- **2026-07-12T13:37:18.004Z** [0.children.1] refined: Refined draft → concrete: Add c8 coverage npm script and c8 devDependency to obsidian-rag/package.json
- **2026-07-12T13:37:19.861Z** [0.children.2] refined: Refined draft → concrete: Add c8 coverage npm script and c8 devDependency to gitevo/package.json
- **2026-07-12T13:37:56.560Z** [0.children.3] refined: Refined draft → concrete: Add CI coverage gate step to static-analysis job that runs c8 coverage with 90% threshold per package and fails if threshold not met
- **2026-07-12T13:41:05.035Z** [1.children.0] refined: Refined draft → concrete: evolve.ts achieves ≥90% statement coverage via TDD tests. Covers: evolve() orchestration, readTargetFiles, saveState/restoreState, captureState, applyPatch, matchSimple edge cases, elite selection, generation loop.
