# Dependencies
node_modules/

# TypeScript (per-package .gitignore ignores dist/ entirely)
*.tsbuildinfo

# Tests & coverage
.nyc_output/
coverage/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Env
.env
.env.local

# Cache
.cache/
.evo/
.serena/

# Stryker
.stryker-tmp/
reports/

# quality-guard: the old per-repo hook baseline. The gate now uses
# .github/quality/quality-baseline.json, which is tracked on purpose.
.quality-baseline.json

# Generated local quality dashboard report.
.quality/

# quality-guard: the one-shot gate waiver. It must never be committed.
.quality-skip

# tighten loop scratch: ledger, quarantine
.tighten/
.data/micro-mutations/stryker.run.json
.data/micro-mutations/queue.json

# blind-rewrite scratch: quarantined original
.blind/

# spec-extract scratch: extracted spec files
.spec-extract/

# skill-migrate scratch: eval cases, sandboxes, benchmark runs
.skill-migrate/

# Scratch clipboard
clipboard.txt

# Plugin orphan-detection timestamp (written by marketplace tooling)
.orphaned_at

# Orphaned pre-marketplace plugin workspace and its coverage output.
packages/dod-guard/

# ste-lint waiver sentinel: written by hand, consumed by ~/.claude/hooks/ste-turn-guard.mjs
.prose-skip

# dod_create scratch: DoD markdown from smoke and anti-cheat test runs
.cli-smoke.md
test-anticheat.md

# openspec archive output
openspec/changes/archive/
# dod-guard task snapshots include the local absolute tasks.md path.
.task-guard.json
# openspec-dashboard process lock
.dashboard.pid
