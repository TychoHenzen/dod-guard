# Context preservation fixture

| Input | Expected capture record |
| --- | --- |
| Two features share the constraint “preserve cited examples”, feature A has decision “use existing issues”, and a final supplied detail fits neither feature. | Both issues retain the shared constraint; A retains the supplied decision in `## Context`; show the final detail as an explicit unresolved remainder and ask where it belongs. |
