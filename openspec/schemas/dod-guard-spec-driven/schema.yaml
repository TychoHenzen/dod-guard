name: dod-guard-spec-driven
version: 1
description: Spec-driven workflow - proposal, specs, design, tasks, dod, steps
artifacts:
  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    instruction: >
      Create the proposal document that establishes WHY this change is needed.


      Sections:

      - **Why**: 1-2 sentences on the problem or opportunity. What problem does
      this solve? Why now?

      - **What Changes**: Bullet list of changes. Be specific about new
      capabilities, modifications, or removals. Mark breaking changes with
      **BREAKING**.

      - **Capabilities**: Identify which specs will be created or modified:
        - **New Capabilities**: List capabilities being introduced. Each becomes a new `specs/<capability-path>/spec.md`. Use kebab-case for path segments you introduce (e.g., `user-auth` or `identity/user-auth`) and follow the project's existing spec organization.
        - **Modified Capabilities**: List existing capabilities whose REQUIREMENTS are changing. Only include if spec-level behavior changes (not just implementation details). Each needs a delta spec file. Use the exact existing path under `openspec/specs/`. Leave empty if no requirement changes.
      - **Impact**: Affected code, APIs, dependencies, or systems.


      IMPORTANT: The Capabilities section is critical. It creates the contract
      between

      proposal and specs phases. Research existing specs before filling this in.

      Each capability listed here will need a corresponding spec file.


      Every change must either declare at least one capability (new or

      modified) or explicitly opt out of specs: `openspec validate` rejects a

      change with zero deltas unless the change's `.openspec.yaml` sets

      `skip_specs: true`. Use `skip_specs: true` only when no spec-level

      behavior changes (pure refactor, tooling, docs) - specs describe

      behavior, so if behavior does not change, no spec should change either.

      Do not invent a requirement just to satisfy validation.


      Keep it concise (1-2 pages). Focus on the "why" not the "how" -

      implementation details belong in design.md.


      This is the foundation - specs, design, and tasks all build on this.
    requires: []
  - id: specs
    generates: specs/**/*.md
    description: Detailed specifications for the change
    template: spec.md
    instruction: >
      Create specification files that define WHAT the system should do.


      A spec is a behavior contract, not an implementation plan.


      Good spec content:

      - Observable behavior users or downstream systems rely on

      - Inputs, outputs, and error conditions

      - External constraints (security, privacy, reliability, compatibility)

      - Scenarios that can be tested or explicitly validated


      Avoid in specs:

      - Internal class/function names

      - Library or framework choices

      - Step-by-step implementation details

      - Detailed execution plans (those belong in design.md or tasks.md)


      Quick test: if the implementation can change without changing externally

      visible behavior, it likely does not belong in the spec.


      Create one spec file per capability listed in the proposal's Capabilities
      section.

      `<capability-path>` is the spec directory relative to `specs/` (for
      example,

      `user-auth` or `identity/user-auth`). Preserve the full path:

      - New capabilities: use the exact path from the proposal at
      `specs/<capability-path>/spec.md`. Any path segment newly introduced in
      the proposal must be kebab-case. Follow the project's existing
      organization; do not add a new domain level when the project uses a flat
      layout.

      - Modified capabilities: use the exact existing path from
      `openspec/specs/<capability-path>/` when creating the delta at
      `specs/<capability-path>/spec.md`. Do not move or rename the capability.


      There must be at least one spec file unless the change's `.openspec.yaml`

      sets `skip_specs: true` (no spec-level behavior change) - `openspec
      validate`

      rejects a zero-delta change without that marker. If the proposal lists no

      capabilities and `skip_specs` is not set, revisit the proposal first.


      Delta operations (use ## headers):

      - **ADDED Requirements**: New capabilities

      - **MODIFIED Requirements**: Changed behavior - MUST include full updated
      content

      - **REMOVED Requirements**: Deprecated features - MUST include **Reason**
      and **Migration**

      - **RENAMED Requirements**: Name changes only - use FROM:/TO: format


      Format requirements:

      - Each requirement: `### Requirement: <name>` followed by description

      - Use SHALL/MUST for normative requirements (avoid should/may)

      - Each scenario: `#### Scenario: <name>` with WHEN/THEN format

      - **CRITICAL**: Scenarios MUST use exactly 4 hashtags (`####`). Using 3
      hashtags or bullets will fail silently.

      - Every requirement MUST have at least one scenario.


      New capabilities only: start the delta spec with a `## Purpose` section -

      one or two sentences (50+ characters, or `openspec validate --strict`

      reports it as too brief) describing what the capability is for. Archive

      copies it into the main spec it creates; without it the new main spec is

      left with a `TBD ... Update Purpose after archive` placeholder to fill in

      by hand. Do NOT add `## Purpose` to a delta for an existing capability -

      that spec already has one and the delta's is ignored. To change an

      existing capability's Purpose - including a leftover `TBD` placeholder -

      edit `openspec/specs/<capability-path>/spec.md` directly.


      MODIFIED requirements workflow:

      1. Locate the existing requirement in
      openspec/specs/<capability-path>/spec.md

      2. Copy the ENTIRE requirement block (from `### Requirement:` through all
      scenarios)

      3. Paste under `## MODIFIED Requirements` and edit to reflect new behavior

      4. Ensure header text matches exactly (whitespace-insensitive)


      Common pitfall: Using MODIFIED with partial content loses detail at
      archive time.

      If adding new concerns without changing existing behavior, use ADDED
      instead.


      Example (a new capability, so it opens with `## Purpose`):

      ```

      ## Purpose


      Lets users take their data out of the product in a portable format.


      ## ADDED Requirements


      ### Requirement: User can export data

      The system SHALL allow users to export their data in CSV format.


      #### Scenario: Successful export

      - **WHEN** user clicks "Export" button

      - **THEN** system downloads a CSV file with all user data


      ## REMOVED Requirements


      ### Requirement: Legacy export

      **Reason**: Replaced by new export system

      **Migration**: Use new export endpoint at /api/v2/export

      ```


      Specs should be testable - each scenario is a potential test case.
    requires:
      - proposal
  - id: design
    generates: design.md
    description: Technical design document with implementation details
    template: design.md
    instruction: >
      Create the design document that explains HOW to implement the change.


      When to include design.md (create only if any apply):

      - Cross-cutting change (multiple services/modules) or new architectural
      pattern

      - New external dependency or significant data model changes

      - Security, performance, or migration complexity

      - Ambiguity that benefits from technical decisions before coding


      Sections:

      - **Context**: Only the current state and constraints needed to explain
      the approach. Reference the proposal for motivation instead of restating
      it (e.g., "See proposal.md - Why").

      - **Goals / Non-Goals**: What this design achieves and explicitly
      excludes. Don't restate the proposal's scope - add only design-level
      boundaries.

      - **Decisions**: Key technical choices with rationale (why X over Y?).
      Include alternatives considered for each decision.

      - **Risks / Trade-offs**: Known limitations, things that could go wrong.
      Format: [Risk] -> Mitigation

      - **Migration Plan**: Steps to deploy, rollback strategy (if applicable)

      - **Open Questions**: Unknowns that can safely be answered later without
        changing the specs, the approach, or the task breakdown. Omit if none.

      Open questions are for genuinely deferrable unknowns, not decisions you

      skipped. If a question would change the specs, the chosen approach, or

      the task breakdown, resolve it now - ask the user instead of guessing.


      Focus on architecture and approach, not line-by-line implementation.

      The proposal covers why and what; design covers how. Reference the

      proposal for motivation and, once written, the specs for requirements -

      if a section would only restate them, point to them instead.


      Good design docs explain the "why" behind technical decisions.
    requires:
      - proposal
  - id: tasks
    generates: tasks.md
    description: Implementation checklist with trackable tasks
    template: tasks.md
    instruction: >
      Create the task list that breaks down the implementation work.


      Before writing tasks, check design.md for Open Questions. If any of them

      would change what gets built, resolve them with the user first - do not

      bake an unstated assumption into the task list.


      **IMPORTANT: Follow the template below exactly.** The apply phase parses

      checkbox format to track progress. Tasks not using `- [ ]` won't be
      tracked.


      Guidelines:

      - Group related tasks under ## numbered headings

      - Each task MUST be a checkbox: `- [ ] X.Y Task description`

      - Tasks should be small enough to complete in one session

      - Order tasks by dependency (what must be done first?)


      Example:

      ```

      ## 1. Setup


      - [ ] 1.1 Create new module structure

      - [ ] 1.2 Add dependencies to package.json


      ## 2. Core Implementation


      - [ ] 2.1 Implement data export function

      - [ ] 2.2 Add CSV formatting utilities

      ```


      Reference specs for what needs to be built, design for how to build it.

      Each task should be verifiable - you know when it's done.
    requires:
      - specs
      - design
  - id: dod
    generates: dod.md
    description: Definition of Done proof tree generated from the change's spec deltas
    template: dod.md
    instruction: |
      Build the proof tree for this change, then sharpen it.

      **Never write dod.md by hand.** Call the `dod_generate` MCP tool with
      this change's id and the repo path. It reads the spec deltas, converts
      them, and registers the document. Proofs live in canonical storage, so
      editing the rendered file cannot change what runs, and the next write
      regenerates the file and discards your edit.

      The delta decides the shape, not you. Every `### Requirement:` becomes
      one group. Every `#### Scenario:` under it becomes one leaf. Three
      requirements with two scenarios each give three groups of two leaves.

      Whether a scenario becomes a machine-checkable leaf turns on one rule:
      does its THEN line hold a backticked code span with a space in it,
      whose first word is a known tool? `npm`, `npx`, `node`, `git`,
      `openspec`, `dod-guard`, `grep`, `findstr`, `tsc` and `biome` are the
      known tools. A scenario that names a real command becomes a concrete
      leaf proved by exit code 0. A scenario that only describes an outcome
      in prose becomes a draft leaf with a `MANUAL:` intent, and a draft
      holds the document at INCOMPLETE. That is the correct "a human still
      owes us something" state.

      So write the proving command into the THEN line while you draft the
      delta. Nothing later promotes prose into a command for you.

      A command that already passes proves nothing. Point a proof at the
      test file or the guard the work still has to write, not at a suite
      that is green today.

      ## After generation

      Every generated leaf carries predicate `exit_code` at 0 and category
      `other`. Sharpen them with `dod_amend`, `dod_refine` and
      `dod_add_node`. A predicate is `{type, value, timeout_ms}`, and these
      10 types are the whole vocabulary:

      | Predicate | Passes when |
      |---|---|
      | `exit_code` | the command exits with `value` |
      | `exit_code_not` | the command exits with anything but `value` |
      | `output_contains` | stdout plus stderr contains the substring `value` |
      | `output_not_contains` | that text does not contain `value` |
      | `output_matches` | that text matches the regex `value` |
      | `output_not_matches` | that text does not match the regex `value` |
      | `tdd` | the command failed on an earlier run, then passes |
      | `adversarial` | the gate for phase `value` is recorded GO |
      | `holdout` | the holdout test fingerprint is unchanged |
      | `convergence` | the phase 4 convergence audit reached GO |

      `category` takes one of four values:

      | Category | Use for |
      |---|---|
      | `behavioral` | a check that the feature does the right thing |
      | `wiring` | a check that the piece is reachable from the real system |
      | `test_audit` | a gate over the tests themselves |
      | `other` | anything the three above do not fit |

      Raise `timeout_ms` above the 120000 default for a slow tool.

      A proof often names a test file nobody has written yet. Amend those to
      `output_matches` with a pattern like `"export.*csv"`, so a reasonable
      naming choice still passes.

      Proof commands run on the host OS. On Windows use `findstr` rather than
      `grep`, and write file paths in full, because cmd.exe expands no globs.

      ## What generation never adds

      The converter adds no company baseline, no integration pair, and no
      inspection leaf. Add all three by hand with `dod_add_node`.

      **Company baseline.** Read `standards/dod-baselines.md` and take the
      minimum proofs for this work type. A bug, defect, regression or
      incident is a bug. A feature, enhancement, refactor or new component is
      general. Reserve minimal for work the user has explicitly held to no
      baseline. Read `standards/language-commands.md` for the command that
      fits this project's language. That file's `manual` predicate column is
      out of date, so add every row it marks manual as a `MANUAL:` draft
      instead. Count the project's existing lint and format violations before
      you start editing. Under 10, demand zero. At 10 or more, scope the
      proof to changed files or assert the count does not rise. A baseline
      row can genuinely fail to apply, because the project has no linter or
      no test runner. Never drop such a row in silence; tell the user.

      **Integration pair.** For every feature the tree covers, add two
      leaves. The wiring leaf greps for the import, the registration or the
      route that makes the piece reachable. The behavioral leaf drives the
      feature through the system's real entry point, not the component's own
      API. Wiring alone catches "registered but broken". Behavior alone
      catches "works in the harness, unreachable in production". Neither may
      become a human step. Place the pair last among the concrete leaves.

      **Human judgement.** There is no human-verification predicate. A step
      only a person can judge is a draft leaf whose `intent` starts with the
      literal prefix `MANUAL:`. Other skills key off that prefix, so write it
      exactly. Never invent a command that pretends to check an opinion. Add
      a `MANUAL:` inspection leaf when the work touches `rendering/`, `ui/`,
      `graphics/`, `shaders/`, `sprites/`, `scenes/` or `levels/`, or when a
      leaf intent mentions render, display, show, look, appear or visual, or
      movement, collision, spawn, ai behavior or gameplay. A build that
      compiles proves nothing about what the screen shows. When in doubt, add
      it. A needless check costs a minute; a missing one ships blind.

      **Test-first pair.** A bug fix needs a regression test written red
      first, and a feature needs unit tests written red first. This is the
      one case where a requirement needs two leaves, because a `tdd` proof
      alone cannot tell a real assertion from `assert true`. Add a leaf whose
      command greps the test file for a real assertion, predicate
      `output_matches`, category `test_audit`. Then add a leaf that runs that
      test, predicate `tdd` at 0, category `behavioral`.

      ## Before you hand off

      Run `dod_check` with no node path, before any code exists. Sort every
      result into two piles. Expected to fail means the command ran and the
      feature is simply absent. Mis-authored means a missing tool, a wrong
      path, a shell error, a placeholder, or a proof that passes already and
      therefore proves nothing. Fix every mis-authored leaf. A clean baseline
      is one where nothing passes yet and nothing failed for the wrong
      reason.
    requires:
      - specs
  - id: steps
    generates: steps.json
    description: Step-by-step execution plan derived from the DoD proof tree
    template: steps.json
    instruction: |
      Write the plan that /dod-guard:step-by-step executes. It lives at
      `openspec/changes/<id>/steps.json` and nowhere else.

      **Do not hand-write the skeleton.** Run `dod-guard steps <change-id>`.
      It reads the DoD registered for this change and derives the fields that
      must never drift from it: one step per concrete leaf, that leaf's own
      proof command as `verify_cmd`, its title as the step title, and a
      dependency on the step before it. A `MANUAL:` draft leaf becomes a step
      with `manual_required` true and an empty `verify_cmd`, because both
      already mean a human still owes us something.

      Then fill in the two fields a machine cannot know. `files` is the paths
      the step reads or may modify. `verify_surface` is one of `code`,
      `config`, `structural`, `visual` or `gameplay`. The command leaves them
      empty and `code`, so an unfilled `files` reads as unfilled rather than
      as decided. Correct the linear dependency chain too, wherever two steps
      are genuinely independent.

      A change with no `dod.md` has no proof tree to derive from, so build
      the steps from `tasks.md` instead, one step per task. That is the pure
      refactor case, where the change sets `skip_specs: true` and no spec
      delta exists to generate a DoD from.

      `templates/steps.json` carries the field list. Read it rather than
      restating it anywhere else. Do not invent a step that matches no DoD
      leaf and no task.
    requires:
      - tasks
apply:
  requires:
    - tasks
    - dod
    - steps
  tracks: tasks.md
  instruction: |
    Read context files, work through pending tasks, mark complete as you go.
    Pause if you hit blockers or need clarification.
