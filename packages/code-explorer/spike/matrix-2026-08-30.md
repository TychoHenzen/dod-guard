# Serena and Symbols matrix, 2026-08-30

This record contains observations only. It does not select a production adapter.

## Repeatable commands

```powershell
npm install --prefix <temporary-directory> --ignore-scripts --no-package-lock @p1va/symbols@0.0.25
$env:CODE_EXPLORER_SPIKE_OUTPUT = "packages/code-explorer/spike/symbols-0.0.25-handshake.raw.json"
node packages/code-explorer/spike/scripts/mcp-handshake.mjs node <temporary-directory>/node_modules/@p1va/symbols/dist/index.js start
serena start-project-server
```

For Serena, send `POST http://127.0.0.1:24225/query_project` with project name
`DeepSeekCustom`, a read-only tool name, and JSON-encoded tool parameters. Set
`PYTHONUTF8=1` before starting Serena on Windows.

## Observed result

Symbols completed the MCP initialize and tools/list handshake in 847 ms. Its direct
Rust run did not complete the bounded DeepSeek search before the 30-second command
budget. Its child later completed after 227,513 ms with no `TranscriptContent` match.
That distinct late result is kept in `symbols-0.0.25-deepseek-search.raw.json`.
Serena returned the expected DeepSeek symbol observations after warm-up, but
its first project initialization took 59,503 ms. Its first `find_symbol` operation
took 26,644 ms. It also saved Rust symbol-cache files in DeepSeekCustom's `.serena`
directory and returned HTML for unsupported tool errors. These are hard-gate failures.

The largest checked-in bounded response text is 177 UTF-8 bytes. Complete upstream
HTTP response bodies were not captured, so the 1 MiB raw-response gate is unverified,
not passed. Serena remains rejected independently for project writes, initialization,
and operation time.

The checked-in raw records are `serena-1.7.0-deepseek.raw.json` and
`symbols-0.0.25-deepseek-timeout.raw.json`. The manifest inventories every fixture
cell and its oracle. Fixture execution did not begin after both dependencies failed
the DeepSeek hard gates. The cells are recorded as zero, not as supported operations.
