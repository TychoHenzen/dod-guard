{
  "name": "quality-guard",
  "version": "0.5.10",
  "description": "Structural code quality gate. The PostToolUse hook gives file-local feedback, while the staged gate makes the commit decision and CI replays it from the committed tree. Ships 1 skill: /quality-refactor (systematic refactoring to an explicit machine-checked quality bar).",
  "author": {
    "name": "tychohenzen",
    "url": "https://github.com/tychohenzen"
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "apply_patch|Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/quality-guard.mjs\"",
            "timeout": 30,
            "statusMessage": "quality-guard: checking structure..."
          }
        ]
      }
    ]
  }
}
